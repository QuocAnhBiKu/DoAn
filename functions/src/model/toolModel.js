class Tool {
    constructor(toolId, toolName, toolDescription, toolVersion, toolTypes) {
      this.toolId = toolId;
      this.toolName = toolName;
      this.toolDescription = toolDescription;
      this.toolVersion = toolVersion;
      this.toolTypes = toolTypes;
    }
  }
  
  module.exports = Tool;
  